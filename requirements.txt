# Minimal dependencies for Krishi Dhan Sahayak Terminal Chatbot
# No external dependencies required for basic chatbot functionality

# Optional: For enhanced terminal interface (recommended)
rich==14.1.0
colorama==0.4.6

# Core runtime
httpx==0.28.1
python-dotenv==1.1.1

# PDF & RAG dependencies
sentence-transformers==5.1.0
pypdf==6.0.0
tqdm==4.67.1
# RAG vector store (optional)
# Note: On Windows, this may require MSVC build tools for the ANN backend.
chromadb==1.0.16

# Optional: Enable Gemini orchestrator (install separately if you want to use it)
google-generativeai==0.7.2
